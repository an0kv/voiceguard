# Optional ML dependencies (for HuggingFace backend / better accuracy)
transformers>=4.42
huggingface_hub>=0.23
safetensors>=0.4

# Torch wheels are large; install CPU build on your platform.
torch>=2.2
